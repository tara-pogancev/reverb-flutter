import 'package:flutter/material.dart';

const defaultBorderRadius = 30.0;
const defaultPagePadding = 15.0;
const smallElementPadding = 10.0;
const defaultElementPadding = 15.0;
const largeElementPadding = 45.0;
const bottomPlayerBarPadding = 150.0;

const smallSpacer = SizedBox.square(dimension: 10);
const defaultSpacer = SizedBox.square(dimension: 20);
const largeSpacer = SizedBox.square(dimension: 30);

const maxSpeed = 1.5;
const minSpeed = 0.5;
const maxPitch = 1.8;
const minPitch = 0.2;
